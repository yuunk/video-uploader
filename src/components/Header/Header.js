import React from 'react';
import './Header.css';

const header = () => {
    return (
        <header className="Header">
            <h1>Video Uploader</h1>
        </header>
    );
}

export default header;